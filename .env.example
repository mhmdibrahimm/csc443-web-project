# Frontend env vars consumed by Vite. Copy to `.env.local` for local dev.
# Each var must be prefixed with VITE_ to be exposed to the browser bundle.

# Base URL of the Velo API (no trailing slash).
VITE_API_URL=http://localhost:3001
